SECRET_KEY=django-insecure-pulse-secret-key-change-in-production-12345
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database configuration (Defaults to SQLite if DATABASE_URL is not set)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/pulse_pms

# Redis configuration for Channels (optional, falls back to in-memory channel layer)
# REDIS_URL=redis://127.0.0.1:6379/0
